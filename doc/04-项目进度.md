# 04 · 项目进度

> 读这份文档的时机：了解项目现在到哪了、接下来做什么。**改完代码请更新本文件。**

## 当前状态（2026-08-27 晚·二次修正）

✅ **落地页事故复盘与回滚**：用户反馈"模拟样式被改坏"，首轮误诊为截图
   太小（换了张三姿势贴纸图）；用户指出真正被毁的是 **v2.1.0 起的前端
   可交互模拟演示**（模拟应用窗口：连接三态/优先级排序/宠物精灵图动画/
   三态托盘卡，Mac 端 08-26 经 API 通道发布的 v2.0→v2.1.0→v2.2.0）。
   我在同机重建"landing v2"时基于旧 base，push 时覆盖掉了这 602 行
   成果。已整体回滚到 cd59366d 并按该页自身规范升 PAGE_VER→2.2.1
   （回滚也必须升版本号，否则被缓存坑住的访客刷不回来）。
   **根因与红线见 05 坑#B7：分叉历史下"reset+reapply 自己的版本"会静默
   覆盖远端新成果——push 前必须 `diff FETCH_HEAD --stat` 审查删减量。**
✅ **v1.9.0 已发版**：组件库采纳（更新 UX 兜底 + 求好评 15 天冷却）；
   **Release 当晚补发**——白天只推了 tag 没建 Release，导致落地页芯片和
   应用内更新检查都还认为最新是 v1.8.4（教训：tag ≠ Release，发完要用
   `releases/latest` 验收）
✅ 落地页 v2.2.1 现状（回滚后）：可交互模拟演示 + 专属关于子页
   airpods-buddy-about.html（含 giscus 反馈讨论区）+ 强制刷新组件
   （页面版本独立于应用版本，防死循环严谨，值得 App 借鉴）

## 待办
- [ ] Mac：.app 打包+图标+签名公证、SMAppService 自启、Phase 2 共享 Web UI

## 当前状态（2026-08-25）

✅ **v1.8.4：WebView2 Runtime 缺失自愈（0x80070002 根治）**：控件不复用
   Edge 浏览器、精简系统常缺 Runtime，旧文案"请装 Edge"是误导（当日在一台
   装着 Edge 151 的机器实证）。现在启动自动探测 → 缺失则引导装官方 2MB
   安装器 → 断网/拒绝时降级借本机 Edge 目录 → 全失败才手动指引。
   同步排掉 `DllCall` 出参传值不回填的隐形炸弹（见 05 C，函数级测试立功）
✅ **本机（Administrator 测试机）已修**：手动装上 Runtime 151.0.4129.107；
   v1.8.4 已编译部署到本机 D:\Downloads\AirPodsBuddy-Windows 并启动验证
   （boot v1.8.4 + navigation ok）
✅ **本副本从 v1.4.0 全量同步到 v1.8.3**（codeload zip 通道；该机 github.com
   443 被墙但 raw/api/codeload 可达，git fetch 暂不可用，**待网络恢复后
   `git pull --rebase` 对齐远端**）；便携 AHK v2 + Ahk2Exe 工具链落 tools/

## 待办
- [ ] github.com 恢复后：pull --rebase 对齐远端 + 推送 v1.8.4 + 发 Release
      （exe 已在 dist/ 备好）
- [ ] 真机回归：宠物窗口在"Edge 降级目录"模式下能出（本机 Runtime 已装，
      降级路径只在函数级验证过，未真机走通）

## 当前状态（2026-08-24）

✅ **仓库更名 `BluetoothDeviceConnector` → `AirPods-Windows`**：描述 /
   topics / 主页链接已更新，名字言简意赅、搜索友好
✅ **更新源与全量链接切换**：`UPDATE_API` / `RELEASE_PAGE` 及 README、
   index.html、产品页 airpods-buddy.html 全部指向新仓库名，下载按钮改为
   zip 直链（旧链接靠 GitHub 重定向仍可达，存量客户端不受影响）
✅ **v1.8.3 Release 发布**：v1.2.0~v1.8.2 的修复从未发过版，v1.1.2 存量
   用户被困在"启动约 3.5 秒弹 `String has no property named "Result"`
   报错框"（病根 [vp2] 探针 v1.2.0 已删但没出门）；本次发版后自动更新
   会把全部修复推给存量用户

## 版本历史

| 版本 | 日期 | 内容 |
|---|---|---|
| v1.0 | 08-18 早 | 首版：托盘 AHK 原生（bluetooth_tray_connector.ahk） |
| v1.1.0 | 08-18 | UI 重构：WebView2 HTML 前端 + 完整界面 |
| v1.1.1/2 | 08-18 | 白屏误诊为 bounds 问题；加自检日志；自动更新 |
| v1.2.0 | 08-19 凌晨 | 大修：白屏/弹窗/JSON/Persistent + 日志系统 |
| v1.3.0 | 08-19 上午 | 体验版：拖动(初版)/优先级/圆润UI/图标内嵌/README 重写 |
| v1.3.1 | 08-19 中午 | 拖动真修复+托盘左键切换+双状态图标；Mac 脚手架；远端推送+共享同步 |
| **v1.4.0** | **08-19 晚** | **学习 Mac：三态表情图标+连接加载动画+独立托盘看门** |
| **v1.5.0** | **08-19 深夜** | **星星布丁宠物弹窗：点托盘从右下角弹出陪你等连接（萌）** |
| **v1.6.0** | **08-19 深夜** | **修左键失效+宠物白屏；前端粉彩萌系重设计；降噪模式入口（实验）** |
| v1.7.0~v1.8.2 | 08-19 | 原生降噪切换（实验）、Apple 风重设计、连接状态误判修复、宠物弹窗四连修 |
| **v1.8.3** | **08-24** | **仓库更名 AirPods-Windows；首发 Release（含 v1.2.0 以来全部修复），根治 v1.1.2 启动报错弹窗** |

## v1.3.1 变更清单

1. **拖动真修复**：WM_NCLBUTTONDOWN 被 WebView2 鼠标捕获拦截 → 改
   `StartWindowDrag()` 手动循环（GetCursorPos+SetWindowPos+GetAsyncKeyState）
2. **托盘左键切换**：Default="⇄ 切换连接"，Click=1（左键直达，右键菜单）
3. **托盘状态图标**：star_pudding_on.ico（绿圈布丁）+ off 版，状态变化
   才切换（防闪烁），A_IconTip 同步
4. **Mac v0.1**：Swift 菜单栏 app（6 个源文件 + README），看门狗机制见 doc/06

## 待办（按优先级）

- [x] ~~`git add + commit` + 推送远端~~ ✅ 2026-08-19 已完成：commit 3a45665
      (v1.3.1) + 8484edb，tag v1.3.1 已推 GitHub；完整副本（含 .git）已
      同步到 `E:\共享\创业\BluetoothDeviceConnector` 供 Mac 端接力
- [ ] **Windows 真机验证**：拖动手感（手动循环版未人工试过）、左键托盘切换、
      真耳机连接/断开/优先级 ▲▼ 实际生效
- [x] ~~**Mac 首次编译**~~ ✅ 2026-08-19：修 2 处编译错误后 `swift build
      -c release` 通过；冒烟测试通过（启动/日志/零弹窗）。
      **下一步：真机跑 doc/06 测试清单**（连接/抗抢/输出锁定，需真
      AirPods，由用户配合验证）
- [ ] 重建 `deploy.ps1`（被安全软件删除，见 05 坑#1）并加白名单
- [x] ~~发布 GitHub Release 页~~ ✅ 2026-08-24：v1.8.3 已发布
      （`AirPodsBuddy-Windows.zip`，资产名与自动更新器匹配规则一致，
      v1.1.2 存量用户点"检查更新"即可升级）
- [ ] Mac：.app 打包+图标+签名公证、SMAppService 自启、Phase 2 共享 Web UI
- [ ] 可选：Windows 输出设备锁定（借鉴 Mac AudioRouter）、设备重命名、
      电量显示（参考 AirPodsDesktop BLE 方案）、最小化行为设置开关
      （当前=缩托盘，用户若不满意改回 myGui.Minimize() 一行）

## 文件清单（改动涉及）

```
airpods_buddy.ahk          主程序（本次大改）
webui/index.html           前端（+jserror 回传，-调试代码）
webui/index_built.html     重新构建
dist/AirPodsBuddy.exe      重新编译 ✓ 已部署
.gitignore                 + logs/
doc/                       新增（本目录）
tools/ahk2exe_stable/      新增（编译器，GitHub 下载）
```
