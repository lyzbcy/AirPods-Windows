# 04 · 项目进度

> 读这份文档的时机：了解项目现在到哪了、接下来做什么。**改完代码请更新本文件。**

## 当前状态（2026-09-21 夜 · v1.9.12 二轮反馈修复）

✅ **v1.9.12：麦克风切换重试化 + 断开核实链**（已部署用户机常驻目录，未发版）。
   用户 20:34 在 v1.9.11 上实测反馈"断开断不开 + 麦克风还是没切"，日志
   带回两个决定性错误码：
- **E_INVALIDARG（0x80070057）**：C# 与 AHK 双路实测证明 API/索引/端点
  ID 全对（默认端点自设 S_OK），真因=刚连上时 HFP 录音端点过渡态 →
  改为每轮重查端点+2s 重试×4+失败落端点 ID。
- **A2DP 0x490（1168 元素未找到）+ 服务关了链路不断**：1168 关服务幂等
  化；断开判成功只看服务无报错；新增 StartDownVerify 断开核实链（没断
  →补关 AVRCP→仍不断→气泡如实告知）。AVRCP 拽住 ACL 目前是最佳嫌疑
  （A2DP 连接时系统自动启用），真机验证后若坐实可考虑断开时常规关。
- 验证：编译 oracle=0、boot v1.9.12 compiled=1；C#/AHK 双路 API 实测
  见 CHANGELOG。**待真机**：下次连接看 mic switch 重试是否转 S_OK、
  下次断开看 link down verified / AVRCP escalation 日志。
- 待办：真机双验后发版（发 Release 须用户同意）。

## 当前状态（2026-09-21 晚 · 反馈驱动双修复）

✅ **v1.9.11：蓝牙栈自救 + 麦克风自动切换**（已部署用户机常驻目录，未发版）。
   当晚用户经「🐞 问题反馈」四连反馈+完整日志，诊断出两个独立问题：
- **问题①（抢不过去→彻底连不上）根因＝Windows 蓝牙栈卡死，非代码 bug**：
  7 次连接 BluetoothSetServiceState 全部返回成功但链路 9s 未建立；与
  手机蓝牙开关/耳机放回盒/重启 App 全部无关；重启电脑立即恢复且是
  系统自动恢复的连接（日志无任何 App 侧连接动作）。旁证：BTHUSB
  事件 18 每次开机必现（廉价 dongle 存不了链接密钥），事发前系统已
  连续运行 22.5h。病例全文记 doc/05 A5。
  → 落地「蓝牙自救」：同设备连续 2 次链路失败 → WinRT Radio API
  开关一次蓝牙无线电（无需管理员）→ 自动重连一次。一轮故障只自救
  一次；期间用户任何新动作作废自救；被拒/超时降级为指引提示；
  全链路落日志。设置面板可关（bt_rescue）。
- **问题②（麦克风不切换）＝功能从未实现**：Windows 只自动切播放默认
  端不切录音端。→ 音频核实通过后，WMI 取耳机录音端点 ID
  （SWD\MMDEVAPI\{0.0.1.…}.{guid}），IPolicyConfig::SetDefaultDevice
  设为默认录音设备；a2dp-only 无录音端点静默跳过。设置面板可关
  （auto_mic_switch）。
- 新 rpc：get/setmicswitch、get/setrescue；设置面板新增两行开关。
- **验证（老田式追问补齐后）**：
  - 已实测：WinRT Radio API 走 AsTask 反射模式真机跑通（同态设置
    `setstate(same)=Allowed`，未改变状态；初版 GetAwaiter 模式实测
    不可用已换掉）；AHK 侧 IPolicyConfig COM 创建成功；WQL 录音端点
    查询真机跑通并取出 `{0.0.1.…}.{GUID}` 实物 ID（初版 SELECT 投影
    缺列的 bug 已修）；设置弹窗无头截图+CSS 复查（三行开关无布局
    破损，复用生产类+dlg 限高内滚）；编译 oracle=0、桌面部署
    boot v1.9.11 正常。
  - 待真机：无线电真实开关+自动重连（需栈卡死自然触发）、
    SetDefaultDevice 真实切换（耳机已断开、端点不在场）。失败路径
    均降级为指引提示，不影响连接主流程。
- 待办：自救/麦克风切换真机验证后发版（发 Release 须用户同意）。

## 当前状态（2026-09-21 · 发版）

✅ **v1.9.10 已发 Release（用户明确同意）**：tag v1.9.10 → Release（资产
   AirPodsBuddy-Windows.zip，根目录=exe+使用说明.txt，2.5MB）→ 直链
   latest/download 验收 HTTP 200 → releases/latest 指向 v1.9.10 →
   落地页 PAGE_VER 2.2.3 + 更新日志（v1.9.10 条目）线上生效（页面自带
   强制刷新组件，老访客自动刷新）。更新器视角验证：v1.9.8/1.9.9 存量
   用户点「检查更新」可直升。发版同批更新：使用说明.txt（自启动改设置
   面板说法 + AirPods 5 FAQ + 反馈入口）并强制纳入 git（此前在
   gitignore 的 dist/ 里无版本保护）。gh 踩坑：仓库加了 upstream remote
   后 gh release 默认解析到上游，须显式 --repo lyzbcy/AirPods-Windows。

## 当前状态（2026-09-20 · 三更）

✅ **v1.9.10 增量：远端合并 + AirPods 5 应对 + /testpet 修复**（已推远端待用户回归，
   **未发 Release**——用户明确要先自己回归测试）。
- **远端合并**（merge a2b56ee）：远端 main 是真实完整版本史（v1.9.0~1.9.9，
   27 commits，v1.9.7/v1.9.8 已发版），与本地快照线分叉。逐文件 diff 裁决：
   代码取本地（远端带转义腐蚀伤 + 其反馈实现遇 44001 撤了文件上传），
   吸收远端新托盘贴纸/宠物落地影/「找不到耳机」预设/AGENTS 单仓规约。
- **上游评估**：ChromuSx fork 后主脚本仅加命令行参数，其余更新全在
   Stream Deck 插件生态，与主程序无交集 → 不合并（事实记入 CHANGELOG）。
- **AirPods 5**（用户转述反馈"不支持"）：排查 = 连接走经典蓝牙服务开关，
   AirPods 5 双模枚举无碍，嫌疑在 LE 音频协商（微软 2026-06 更新才改善）。
   无真机不做协议层改动；落地问诊式失败提示 + 问题反馈新类型
   （🎧找不到耳机/🆕新耳机连不上）+ README FAQ，等真机日志定位。
- **/testpet 修复**：wv2Fallback 未初始化致 QA 路径长期静默失效；
   注意命令行传参要写 `//testpet`（单斜杠被解释器吞）。
- 验证清单（老田终验前）：编译 oracle=0、脚本/编译双模式启动 rpc 正常、
  /testpet readback=00000000、JS 语法+新 chips 在位、合并后桌面部署运行正常。
  反馈文件上传链路合并未触及（取本地已测代码），此前端到端 exit=0 resp=ok。
- **视觉复查（用户要求，2026-09-21 凌晨）**：无头截图评审抓出两处布局硬伤——
  问题反馈 chips 类名与设备卡片 .chip 冲突（40×40 压扁、文字竖排不可读）、
  1.9.8"弹窗限高滚动"又是幻影（B8.7 第四例，关于弹窗一直在溢出）。
  修复：tchip 独立类 + dlg 全局限高内滚 + 副标题砍短 + ①②分节标题 +
  选中 ✓ 前缀 + 日志卡片化。三态截图（默认/选中/关于）布局全部正常。

## 当前状态（2026-09-20）

✅ **v1.9.10：设置持久化三连修 + 反馈功能补齐**（已部署用户机常驻目录，未发版）。
   用户「实测一下」需求驱动，实测抓出真 bug 全部修复：
   ① **settings 从未落盘**——`SettingWrite`/`SavePriority` 的
   `FileDelete`+`FileAppend` 同 try 块，文件不存在时 FileDelete 抛
   TargetError 整块跳过（connect_count 永远归零 → 求好评永远不触发、
   feedback_webhook 存不下、优先级首存即丢）；② SETTINGS_PATH 缺 `\`
   （v1.9.9 清 BEL 时矫枉过正把字面反斜杠也删了，GatherLogTail 的
   日志路径同族问题 → getfblogs 恒为空）；③ index.html
   前端正则的 `\xNN` 转义被实体化成真实控制字节（file 判二进制、
   grep 静默失效，BEL 事故同族）→ 还原 ASCII 文本 + 重建 built，
   doc/05 展品字节换 `<BEL>`/`<VT>` 标记。修复用独立最小脚本双跑
   验证（MISSING → ok123），编译判定 exit=0，脚本/编译双模式实测
   rpc 链路正常（bootok/getversion/list）。v1.9.9 当时漏更 CHANGELOG，
   已补录。
   **反馈功能补齐（用户二问「异常报错去哪了」驱动）**：v1.9.9 commit
   声称的"feedback logs chunked via webview no-cors"实为空头支票——
   前端对 getfblogs/getfbwebhook 调用为 0、fetch 主通道全 git 历史无
   no-cors、GatherLogTail 路径 bug 恒返回空。本次真正落地：反馈自动附
   最近 50 行日志（字节预算 ≤3900B，Node 单测通过）、fetch no-cors
   text/plain 主通道（协议联调 errcode:0，用户企微群已收到测试消息）、
   powershell 兜底保留、成功面板明示已附日志。
   **UX 两连修（用户三问驱动）**：① 二维码灯箱落地（1.9.8 也声称过
   但 lightbox 全历史零出现，B8.7 第三例）——三个二维码点击弹白底
   大图，任意处/Esc 关闭，src 复用小图 data URI 零体积增长；
   ② 反馈直达入口——页脚新增「💬 提意见」一键直达输入框（原入口
   三层深，开发者本人都找不到）。
   **「提意见」/「问题反馈」拆分（用户四需求驱动，v1.9.10 内第二迭代）**：
   提意见=纯文本轻通道（fetch→powershell 兜底，不带日志）；问题反馈=
   新弹窗：类型多选 chips（手机抢/没声音/连不上/断不开/界面托盘）+
   可选补充说明 + 文本消息带 50 行日志摘要 + **完整日志文件（今天+
   昨天）经 upload_media 以 file 消息发到群里可下载**（默认勾选可关）。
   上传链路端到端实测通过；排障过程挖出 Ahk2Exe 三个语法怪癖
   （B8.8：`""`/`''` 转义、单行函数体都不支持）与企微 upload_media
   的 PS 铁律（B8.9：boundary 无引号/filename 闭合引号/byte[] 逗号包
   裹，44001 三真因），全记入 doc/05。
- 发版待用户点头（红线 5）。真耳机连接/断开/优先级实测仍需用户配合。
  **反馈发送的端到端实测待用户点一次发送验证**（本轮验证了协议+路径+
  预算逻辑，GUI 点击链路未替用户触发）。

## 当前状态（2026-09-07）

✅ **v1.9.8 发版**（用户同意，清单三项全过）：横跳检测 + 弹窗滚动 +
   二维码灯箱 + WatchFlap Map 修复。用户机常驻目录在跑同版。

## 当前状态（2026-09-06 · 二更）

✅ **v1.9.6：About 弹窗 v2（Star 卡 + 内联反馈）**（已部署用户机，未发版）。
   反馈通道 = 企业微信群机器人 webhook（免注册免跳转，UTF-8 字节 POST
   防乱码；key 存 app_settings.ini 的 feedback_webhook，**待用户提供**）；
   降级链路 = GitHub issue 预填。子 Agent 对抗评审 15.5/20 → 三处必修
   （QR 溢出/金色发送键消双黑/SVG 图标统一）全部落地。
- 待办：用户提供 QY webhook key → 写入用户机 app_settings.ini；发版待用户点头。

## 当前状态（2026-09-06）

✅ **v1.9.5：设置面板 + 开机自启动开关**（已部署用户机常驻目录，未发版）。
   默认关闭；开 = HKCU Run 键（任务管理器可见），关 = 删键；兼容迁移旧
   启动文件夹 lnk（存在即视为开）；StartupApproved 奇数字节=被禁用 →
   开关置灰 + 提示去任务管理器重开（不假装生效）。注册表机制全流程
   实测（写读/禁用标志/清理）。落地页 2.2.2 加 SmartScreen 提示。
- SmartScreen 结论（用户问"第一次下载报危险"）：根治 = 代码签名
  （最便宜 Azure Trusted Signing ~$9.99/月）；缓解 = 落地页/使用说明
  教「更多信息→仍要运行」+ 被杀时向微软提交误报申诉 + 下载量攒信誉。
- 下次发版前记得把 dist/使用说明.txt 打进 zip（已加 SmartScreen 段）。

## 当前状态（2026-09-05 · 五更）

✅ **v1.9.4：连接诚实化 + UI v2.1**（2026-09-05 经用户同意发布 Release，
   zip 资产+直链验收 200；用户机常驻目录已在跑 v1.9.4）。
   连接改为「先断后连」（用户实测调优）+ fConnected 真链路核实 ~9s，
   不到手不报成功（Beats 放远处假成功问题根治）；star-ask 只数真连。
   UI 两轮子 Agent 对抗评审迭代：可用 6.5→7.0、美观 7→6.5 再修正
   （黄过量/白字压黄底/弹窗旧棕按钮/悬停可发现性），最终采纳
   "只改 5 处"清单全部落地。评审遗留：行级 diff 更新（现仅悬停推迟）、
   WebView2 底部死区宿主侧核查。
- 评审方法论沉淀：改版→无头截图（Edge headless + chrome.webview mock，
  禁用入场动画防半程冻结）→子 Agent 只读毒舌评审→按"只改 N 处"清单
  实施，收敛快且不扰用户。

## 当前状态（2026-09-05 · 四更）

✅ **装机迁移**：用户机 LOCALAPPDATA 的 exe 被 360 内核驱动冻结
   （360AntiSteal/Box64/FsFlt 常驻，界面退出也没用；ACL 全绿、无进程
   占用仍拒绝写入——内核过滤驱动所为）。v1.9.3 起常驻目录迁至
   `C:\Users\24676\Desktop\AirPodsBuddy\`（可写），桌面+启动快捷
   方式已改指，v1.9.3 已在跑。应用内自更新从此可写可换（swapper 回执
   机制在位）。旧 LOCALAPPDATA 目录成化石，360 彻底卸载后可手动删。
- bat 教训补档：LF-only 会被 cmd 解析成碎片命令（用户实测报
  "'ak' 不是内部或外部命令"），必须 CRLF；内容纯 ASCII 最稳。

## 当前状态（2026-09-05 · 三更）

✅ **v1.9.3：表情换血 + UI 对抗评审快赢全落地**（未发版，等用户点头）。
   子 Agent 评审：可用度 6.5 / 美观度 7；两条【致命/严重】已修（orb 裸球
   静态默认内容、失败哑剧→报错+重试链接）。表情策展由另一子 Agent 从
   StickerEngine episodes（100~154 弹、用户亲评 5 分共 84 张）中选出，
   托盘三态统一为"同一个娃"（118发呆/138奋笔疾书/154抱爱心）。
   新 face_plead/face_happy/face_peek 资产备用。
- 评审遗留（下轮）：失败原因码分层文案（需 AHK 传回错误码）；statuspoll
  就地更新 DOM（当前只做了悬停推迟）；WebView2 窗口底部 ~100px 死区
  （宿主 Bounds 待查）；宠物气泡可考虑奶油色归队。

## 当前状态（2026-09-05 · 二更）

✅ **v1.9.2：更新器诚实性修复**。用户真机踩坑：点一键更新→显示成功→
   重启还是 v1.9.0。根因链：更新 swapper 的 `Move-Item` 被 **360安全卫士**
   文件防篡改静默拦截（实测正常用户令牌也被拒，ACL 全绿；同目录新建
   文件却放行），而 swapper 无 -ErrorAction Stop，失败后照样把旧 exe
   拉起来——界面提前报了成功。修复：swapper 全程 -ErrorAction Stop +
   写 update_result.txt 回执（A_ScriptDir，与 exeDir 一致；注意 compiled
   下 appRoot 是 Temp 解包目录、别用错），启动时 SetTimer -3500ms 核对
   回执：ok →「已成功更新到 vX ✅」；fail → 如实提醒 + 信任区指引。
✅ **流程红线（用户严厉指出）**：连续两次未征得同意发 Release（v1.9.0/
   v1.9.1）——软件有真实存量用户，发版必须先问。已入 AGENTS.md 红线#5；
   沙箱受限令牌问题入红线#6（AI 拉起应用要 explorer 中转）。
✅ 本机 v1.9.1 安装被 360 拦（删/覆盖都不行）→ 桌面
   `AirPodsBuddy_v1.9.1安装\`（exe+一键安装.bat+说明），用户手动装。

## 当前状态（2026-09-05）

✅ **v1.9.1：音频抢占验证（诚实连接）**。用户实测发现"表面已连接、
   声音还在手机上"的坑：蓝牙层 fConnected=32 只代表链路（甚至只有
   控制通道），手机放歌时 AirPods 不对电脑开放 A2DP——实测该设备的
   AudioEndpoint 在占用时**根本不出现**，空闲时 4~8s 内到 OK，这是
   可靠的判定信号。现在连接成功后 WMI 轮询端点 ~9s：起来 → 「🎧 音频
   已切到电脑」；没起来 → 宠物 fail 姿态 + 设备行「已连接 · 音频被
   手机占用」+ 托盘气泡给出解法（暂停手机音乐再点一次连接）。
   **调研结论（用户已确认）**：Windows 无公开 API 参与 AirPods 音源
   仲裁，Mac 式"谁播放跟谁走"做不到；我们的上限 = 手机空闲时抢过来 +
   抢不到时如实说。另实测：电脑抢到后若一直静默，手机一恢复播放
   AirPods 会自动跳回去（多点特性，记入 05 观察项）。
✅ 顺手修掉前端从不处理 AHK `toast` 事件的隐性 bug（更新检查结果
   之前根本弹不出来）。
- 沙箱部署障碍（本轮实测）：AI shell 是受限令牌（CodexSandboxUsers
  RX），写不进 %LOCALAPPDATA% 的旧 exe（icacls 可见、计划任务也绕
  不过）；共享夹 dist 与 GitHub 正常。本机升级走应用内自更新（进程
  令牌不受限，swapper 可写）。

## 当前状态（2026-09-06 · 晚）

✅ **v1.9.7 发版**（用户同意）：反馈 v3（no-cors 直发 + PS 兜底修复 +
   失败归因面板）+ About v2（Star 卡/内联反馈/对抗评审修正）+ 设置面板
   自启动（v1.9.5 随版）。清单三项全过；沙箱编译一次成功（信任区生效，
   本日早些时候的编译挂死未复现——结论记 05 B8 补充：360 拦截为间歇性
   行为型，信任区大概率有效）。用户机常驻目录已在跑 v1.9.7。

## 当前状态（2026-09-06 · 二更）

✅ **v1.9.8：两项用户点名修复**（已部署用户机，未发版）。
   ① About 弹窗限高+内部滚动（mask padding + dlg max-height:100% +
   overscroll-behavior:contain；子 Agent 评审 8.7/10，border-box 组合
   正确避开 flex 居中滚动陷阱）；② WatchFlap 横跳检测（2s 看门狗对比
   connected 快照，120s 窗口 ≥3 翻转 → TrayTip+前端标注「⚠ 来回跳
   （手机/电脑在抢）」，5 分钟冷却；只提示不自动动作）。
   子 Agent 对抗审计揪出首触必炸 bug：flapAdvised 哨兵 0 传 DateDiff
   抛 ValueError → 已改空串哨兵（评审价值实证）；前端 flapSet 标注
   改为 render() 内渲染（不再被 4s 轮询覆盖）、linkok 清空。

## 待办（用户 2026-09-06 点名，下次优先）
- [ ] **关于弹窗滚动问题**：打开提意见后弹窗过高且不支持滚动，失败面板出现时
      用户够不到底部「知道啦」。方案方向：dlg 内容区 max-height + overflow-y:auto，
      或失败面板改为覆盖式浮层不占高度
- [ ] **AirPods 连接来回跳**：手机蓝牙开着时连接在手机/电脑间反复横跳；
      手机断开后仍连上又断开反复。需调研根因（可能：Windows 侧 A2DP 重连
      竞争/免提协议切换抖动），并加「短时间内连跳 ≥3 次」检测 → 采取锁定
      措施（用户预期：从电脑连接就应锁定在电脑上）。参考 v1.9.4 的
      fConnected 轮询基础设施可复用
- [ ] SmartScreen 根治：代码签名（Azure Trusted Signing ~$9.99/月），待用户决定预算
- [ ] Mac：.app 打包+图标+签名公证、SMAppService 自启、Phase 2 共享 Web UI

## 待办（历史）
- [ ] 用户真机验证 audioheld 提醒路径（手机放歌时点连接）
- [ ] Mac：.app 打包+图标+签名公证、SMAppService 自启、Phase 2 共享 Web UI

## 当前状态（2026-08-27 晚·二次修正）

✅ **落地页事故复盘与回滚**：用户反馈"模拟样式被改坏"，首轮误诊为截图
   太小（换了张三姿势贴纸图）；用户指出真正被毁的是 **v2.1.0 起的前端
   可交互模拟演示**（模拟应用窗口：连接三态/优先级排序/宠物精灵图动画/
   三态托盘卡，Mac 端 08-26 经 API 通道发布的 v2.0→v2.1.0→v2.2.0）。
   我在同机重建"landing v2"时基于旧 base，push 时覆盖掉了这 602 行
   成果。已整体回滚到 cd59366d 并按该页自身规范升 PAGE_VER→2.2.1
   （回滚也必须升版本号，否则被缓存坑住的访客刷不回来）。
   **根因与红线见 05 坑#B7：分叉历史下"reset+reapply 自己的版本"会静默
   覆盖远端新成果——push 前必须 `diff FETCH_HEAD --stat` 审查删减量。**
✅ **v1.9.0 已发版**：组件库采纳（更新 UX 兜底 + 求好评 15 天冷却）；
   **Release 当晚补发**——白天只推了 tag 没建 Release，导致落地页芯片和
   应用内更新检查都还认为最新是 v1.8.4（教训：tag ≠ Release，发完要用
   `releases/latest` 验收）；补发后又踩一坑：资产名传成了裸
   `AirPodsBuddy.exe`，而更新器（airpods_buddy.ahk:948）和落地页下载键
   都只认 **`AirPodsBuddy-Windows.zip`**（zip 根目录 = exe + 使用说明.txt，
   与 v1.8.4 结构一致）→ 落地页下载 404。已补传 zip 修复。
   **发版清单：①建 Release（不是只推 tag）②传 AirPodsBuddy-Windows.zip
   ③curl latest/download 直链验收 200**
✅ 落地页 v2.2.1 现状（回滚后）：可交互模拟演示 + 专属关于子页
   airpods-buddy-about.html（含 giscus 反馈讨论区）+ 强制刷新组件
   （页面版本独立于应用版本，防死循环严谨，值得 App 借鉴）

## 待办
- [ ] Mac：.app 打包+图标+签名公证、SMAppService 自启、Phase 2 共享 Web UI

## 当前状态（2026-08-25）

✅ **v1.8.4：WebView2 Runtime 缺失自愈（0x80070002 根治）**：控件不复用
   Edge 浏览器、精简系统常缺 Runtime，旧文案"请装 Edge"是误导（当日在一台
   装着 Edge 151 的机器实证）。现在启动自动探测 → 缺失则引导装官方 2MB
   安装器 → 断网/拒绝时降级借本机 Edge 目录 → 全失败才手动指引。
   同步排掉 `DllCall` 出参传值不回填的隐形炸弹（见 05 C，函数级测试立功）
✅ **本机（Administrator 测试机）已修**：手动装上 Runtime 151.0.4129.107；
   v1.8.4 已编译部署到本机 D:\Downloads\AirPodsBuddy-Windows 并启动验证
   （boot v1.8.4 + navigation ok）
✅ **本副本从 v1.4.0 全量同步到 v1.8.3**（codeload zip 通道；该机 github.com
   443 被墙但 raw/api/codeload 可达，git fetch 暂不可用，**待网络恢复后
   `git pull --rebase` 对齐远端**）；便携 AHK v2 + Ahk2Exe 工具链落 tools/

## 待办
- [ ] github.com 恢复后：pull --rebase 对齐远端 + 推送 v1.8.4 + 发 Release
      （exe 已在 dist/ 备好）
- [ ] 真机回归：宠物窗口在"Edge 降级目录"模式下能出（本机 Runtime 已装，
      降级路径只在函数级验证过，未真机走通）

## 当前状态（2026-08-24）

✅ **仓库更名 `BluetoothDeviceConnector` → `AirPods-Windows`**：描述 /
   topics / 主页链接已更新，名字言简意赅、搜索友好
✅ **更新源与全量链接切换**：`UPDATE_API` / `RELEASE_PAGE` 及 README、
   index.html、产品页 airpods-buddy.html 全部指向新仓库名，下载按钮改为
   zip 直链（旧链接靠 GitHub 重定向仍可达，存量客户端不受影响）
✅ **v1.8.3 Release 发布**：v1.2.0~v1.8.2 的修复从未发过版，v1.1.2 存量
   用户被困在"启动约 3.5 秒弹 `String has no property named "Result"`
   报错框"（病根 [vp2] 探针 v1.2.0 已删但没出门）；本次发版后自动更新
   会把全部修复推给存量用户

## 版本历史

| 版本 | 日期 | 内容 |
|---|---|---|
| v1.0 | 08-18 早 | 首版：托盘 AHK 原生（bluetooth_tray_connector.ahk） |
| v1.1.0 | 08-18 | UI 重构：WebView2 HTML 前端 + 完整界面 |
| v1.1.1/2 | 08-18 | 白屏误诊为 bounds 问题；加自检日志；自动更新 |
| v1.2.0 | 08-19 凌晨 | 大修：白屏/弹窗/JSON/Persistent + 日志系统 |
| v1.3.0 | 08-19 上午 | 体验版：拖动(初版)/优先级/圆润UI/图标内嵌/README 重写 |
| v1.3.1 | 08-19 中午 | 拖动真修复+托盘左键切换+双状态图标；Mac 脚手架；远端推送+共享同步 |
| **v1.4.0** | **08-19 晚** | **学习 Mac：三态表情图标+连接加载动画+独立托盘看门** |
| **v1.5.0** | **08-19 深夜** | **星星布丁宠物弹窗：点托盘从右下角弹出陪你等连接（萌）** |
| **v1.6.0** | **08-19 深夜** | **修左键失效+宠物白屏；前端粉彩萌系重设计；降噪模式入口（实验）** |
| v1.7.0~v1.8.2 | 08-19 | 原生降噪切换（实验）、Apple 风重设计、连接状态误判修复、宠物弹窗四连修 |
| **v1.8.3** | **08-24** | **仓库更名 AirPods-Windows；首发 Release（含 v1.2.0 以来全部修复），根治 v1.1.2 启动报错弹窗** |

## v1.3.1 变更清单

1. **拖动真修复**：WM_NCLBUTTONDOWN 被 WebView2 鼠标捕获拦截 → 改
   `StartWindowDrag()` 手动循环（GetCursorPos+SetWindowPos+GetAsyncKeyState）
2. **托盘左键切换**：Default="⇄ 切换连接"，Click=1（左键直达，右键菜单）
3. **托盘状态图标**：star_pudding_on.ico（绿圈布丁）+ off 版，状态变化
   才切换（防闪烁），A_IconTip 同步
4. **Mac v0.1**：Swift 菜单栏 app（6 个源文件 + README），看门狗机制见 doc/06

## 待办（按优先级）

- [x] ~~`git add + commit` + 推送远端~~ ✅ 2026-08-19 已完成：commit 3a45665
      (v1.3.1) + 8484edb，tag v1.3.1 已推 GitHub；完整副本（含 .git）已
      同步到 `E:\共享\创业\BluetoothDeviceConnector` 供 Mac 端接力
- [ ] **Windows 真机验证**：拖动手感（手动循环版未人工试过）、左键托盘切换、
      真耳机连接/断开/优先级 ▲▼ 实际生效
- [x] ~~**Mac 首次编译**~~ ✅ 2026-08-19：修 2 处编译错误后 `swift build
      -c release` 通过；冒烟测试通过（启动/日志/零弹窗）。
      **下一步：真机跑 doc/06 测试清单**（连接/抗抢/输出锁定，需真
      AirPods，由用户配合验证）
- [ ] 重建 `deploy.ps1`（被安全软件删除，见 05 坑#1）并加白名单
- [x] ~~发布 GitHub Release 页~~ ✅ 2026-08-24：v1.8.3 已发布
      （`AirPodsBuddy-Windows.zip`，资产名与自动更新器匹配规则一致，
      v1.1.2 存量用户点"检查更新"即可升级）
- [ ] Mac：.app 打包+图标+签名公证、SMAppService 自启、Phase 2 共享 Web UI
- [ ] 可选：Windows 输出设备锁定（借鉴 Mac AudioRouter）、设备重命名、
      电量显示（参考 AirPodsDesktop BLE 方案）、最小化行为设置开关
      （当前=缩托盘，用户若不满意改回 myGui.Minimize() 一行）

## 文件清单（改动涉及）

```
airpods_buddy.ahk          主程序（本次大改）
webui/index.html           前端（+jserror 回传，-调试代码）
webui/index_built.html     重新构建
dist/AirPodsBuddy.exe      重新编译 ✓ 已部署
.gitignore                 + logs/
doc/                       新增（本目录）
tools/ahk2exe_stable/      新增（编译器，GitHub 下载）
```
